- name: Install packages via Nix
  become: false  # Run as the user, not root
  shell: nix-env -iA nixpkgs.{{ item }}
  loop: "{{ nix_packages }}"
  args:
    executable: /bin/bash
  environment:
    PATH: "{{ ansible_env.PATH }}:/home/{{ lookup('env', 'USER') }}/.nix-profile/bin"
